[project]
name = "ascii-art-ify"
version = "0.1.0"
description = "A simple web application to convert images into ASCII art."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.2",
    "flask>=3.1.3",
    "pillow>=12.1.1",
]
